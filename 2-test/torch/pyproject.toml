[project]
name = "torch_test"
version = "0.0.1"
requires-python = ">=3.10"
dependencies = [
    "pandas>=2.3.3",
    "scikit-learn>=1.7.2",
]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
    "torch>=2.9.0",
]
